<?php

// Each module under Modules/ registers its own /api/v1 routes via its
// service provider's boot() method (see Modules/Identity/Providers/
// IdentityServiceProvider for an example). This file intentionally stays
// empty so route ownership lives next to the module it belongs to.
